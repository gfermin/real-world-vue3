<script setup>
//Gets the even data from the parent(HomeView)
defineProps({
  event: {
    type: Object,
    required: true
  }
})
</script>

<template>
  <!--
    -Takes the even object from the props to fill out the data we need
    -Router: binding the name of the route and passing the id as a parameter to make the routing dynamic
  -->
  <RouterLink class="event-link" :to="{ name: 'event-details', params: { id: event.id } }">
    <div class="event-card">
      <h2>{{ event.title }}</h2>
      <span>@{{ event.time }} on {{ event.date }}</span>
    </div>
  </RouterLink>
</template>

<style scoped>
.event-card {
  padding: 20px;
  width: 250px;
  cursor: pointer;
  border: 1px solid #39495c;
  margin-bottom: 18px;
}
.event-card:hover {
  transform: scale(1.01);
  box-shadow: 0 3px 12px 0 rgba(0, 0, 0, 0.2);
}
.event-link {
  color: #2c3e50;
  text-decoration: none;
}
</style>
